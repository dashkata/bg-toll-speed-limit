name: data
description: Data module
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain
  utils:
    path: ../libs/utils

  dio: ^5.8.0
  dio_smart_retry: ^7.0.1

  json_annotation: ^4.8.1

  collection: ^1.19.1

  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable: ^6.7.1
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/data/highways_usa.json
    - assets/data/highways_usa.json
